layout:
    actions:
        - @addTree:
            items:
                product_index_page:
                    blockType: container
                product_grid_container:
                    blockType: container
                product_grid:
                    blockType: datagrid
                    options:
                        grid_name: frontend-products-grid
            tree:
                content:
                    product_index_page:
                        product_grid_container:
                            product_grid: ~
