{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEntityConfigBundle::macros.html.twig' as entityConfig %}
{% import 'OroB2BProductBundle::image_macros.html.twig' as Image %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('orob2b.product.sku.label'|trans, product.sku) }}
            {{ UI.renderProperty('orob2b.product.names.label'|trans, product.defaultName) }}
            {{ UI.renderHtmlProperty('orob2b.product.inventory_status.label'|trans, product.inventoryStatus) }}

            {% if product.images.count %}
                {{ UI.renderHtmlProperty(
                'orob2b.product.images.label'|trans,
                Image.renderProductImages(product.images, imageTypes)
                ) }}
            {% endif %}

            {% set primaryUnitPrecisionElement %}
                {{ 'orob2b.product.productunitprecision.representation'|transchoice(
                product.primaryUnitPrecision.precision,
                {
                    '{{ label }}': ('orob2b.product_unit.'~ product.primaryUnitPrecision.unit.code ~'.label.full')|trans
                }
                ) }}
            {% endset %}
            {{ UI.renderHtmlProperty('orob2b.product.primary_unit_precision.label'|trans, primaryUnitPrecisionElement )}}

            {% set productUnitPrecisions = [] %}

            {% if product.additionalUnitPrecisions.count %}
                {% set titles = ['orob2b.product.productunitprecision.unit.label'|trans,
                'orob2b.product.productunitprecision.precision.label'|trans,
                'orob2b.product.productunitprecision.of'|trans ~ ' ' ~('orob2b.product_unit.'~ product.primaryUnitPrecision.unit.code ~'.label.full')|trans,
                'orob2b.product.productunitprecision.sell.label'|trans]
                %}
                {% set rows = [] %}
                {% for unitPrecision in product.additionalUnitPrecisions %}
                    {% set sell = unitPrecision.sell ?
                        'orob2b.product.productunitprecision.sell_yes.label'|trans
                        :
                        'orob2b.product.productunitprecision.sell_not.label'|trans
                    %}
                    {% set row = [('orob2b.product_unit.'~ unitPrecision.unit.code ~'.label.full')|trans,
                                    unitPrecision.precision,
                                    unitPrecision.conversionRate,
                                    sell]
                    %}
                    {% set rows = rows|merge([row]) %}
                {% endfor %}
                {{ UI.renderHtmlProperty(
                    'orob2b.product.additional_unit_precisions.label'|trans,
                     UI.renderTable(titles,rows,"grid table-hover table table-bordered unit-table"))
                 }}
            {% endif %}
        </div>
        <div class="responsive-block">
            {{ entityConfig.renderDynamicFields(product) }}
        </div>
    </div>
</div>
