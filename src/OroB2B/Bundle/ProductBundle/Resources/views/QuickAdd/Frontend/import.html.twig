{% set pageComponentOptions = {
    '_wid': app.request.get('_wid')
} %}
<div class="widget-content"
     data-page-component-module="orob2bfrontend/js/app/components/widget-form-component"
     data-page-component-options="{{ pageComponentOptions|json_encode }}"
        >
    {% if form.vars.errors|length > 0 %}
        <div class="alert alert-error">
            {{ form_errors(form) }}
        </div>
    {% endif %}

    <form id="{{ form.vars.id }}" name="{{ form.vars.name }}" action="{{ app.request.uri }}" {{ form_enctype(form) }}
          method="{{ form.vars.method }}" data-collect="true" class="form-dialog">
        <fieldset class="form-horizontal">

            <div class="row-oro" >


                <h3>Import Items</h3>

                <div class="alert alert-info import-notice">
                    <strong>Important</strong>:
                    Only correct data will be imported. Incorrect data will be ignored
                </div>

                <h5>Follow the steps below to import multiple products using an CSV file.</h5>


                <ol>
                    <li>Click here to download <a href="#">the CSV template</a></li>
                    <li>Enter item numbers in Column A and quantities needed in Column B. Please do not include any prefixes, dashes, or other punctuation in the item numbers.</li>
                    <li>Save the file on your computer. CSV format (.csv) is recommended and click Upload.</li>
                    <li>{{ form_widget(form.products) }} {{ form_errors(form.products) }}</li>
                </ol>

                <div class="widget-actions">
                    <span class="action-wrapper">
                    <button type="reset" class="btn">Cancel</button>
                        </span>
                        <span class="action-wrapper">
                    <a type="submit"
                            class="btn btn-primary">Upload</a>
                            </span>
                </div>

            </div>

        </fieldset>

        <div class="hidden">
            {{ form_rest(form) }}
        </div>


    </form>
    {{ oro_form_js_validation(form) }}
</div>
