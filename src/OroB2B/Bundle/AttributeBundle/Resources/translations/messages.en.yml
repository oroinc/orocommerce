orob2b:
    attribute:
        form:
            fallback:
                empty: 'Choose fallback...'
                parent_locale: 'Parent locale value'
                default: 'Default value'

        default: 'Default'
