{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.buttonType({
    type: 'submit',
    label: 'orob2b.order.btn.add_to_order'|trans,
    class: 'component-button',
    data: {
        'component-name': 'orob2b_order_quick_add_processor'
    }
}) }}
