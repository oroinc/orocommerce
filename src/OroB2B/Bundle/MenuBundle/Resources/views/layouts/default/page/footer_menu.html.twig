{% block _footer_menu_container_widget %}
    {% set attr = attr|merge({ class: attr.class|default('') ~ " row" }) %}

    <div {{ block('block_attributes') }}>
        <div class="col-md-8 col-sm-12 clearfix">
            {{ block_widget(block) }}
        </div>
    </div>
{% endblock %}

{% block _footer_menu_information_widget %}
    {% for child in item.children %}
        <div class="footer-nav footer-nav_1-3">
            <h2 class="footer-nav__title">{{ child.label }}</h2>
            {% set item = child %}

            {{ block('menu_widget') }}
        </div>
    {% endfor %}
{% endblock %}

{% block _footer_menu_itemwhy_widget %}
    {% for child in item.children %}
        <div class="footer-nav footer-nav_1-3">
            <h2 class="footer-nav__title">{{ child.label }}</h2>
            {% set item = child %}

            {{ block('menu_widget') }}
        </div>
    {% endfor %}
{% endblock %}

{% block _footer_menu_account_widget %}
    {% for child in item.children %}
        <div class="footer-nav footer-nav_1-3">
            <h2 class="footer-nav__title">{{ child.label }}</h2>
            {% set item = child %}

            {{ block('menu_widget') }}
        </div>
    {% endfor %}
{% endblock %}
