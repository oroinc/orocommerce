{% import 'OroUIBundle::macros.html.twig' as UI %}
{% if resource_granted('EDIT', entity) %}
    {{ UI.clientButton({
        'dataUrl': path('orob2b_warehouse_inventory_level_view', {'id': entity.id}),
        'aCss': 'no-hash btn',
        'iCss': 'icon-list-alt',
        'label' : 'orob2b.warehouse.widgets.inventory_management'|trans,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
                'dialogOptions' : {
                    'stateEnabled': false,
                    'incrementalPosition': false,
                    'title' : 'orob2b.warehouse.widgets.inventory_management'|trans,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 1000,
                    'height': 400,
                }
            }
        }
    }) }}
{% endif %}
