{% import 'OroUIBundle::macros.html.twig' as UI %}

{%- if entity and entity.category and resource_granted('EDIT', entity.category) -%}
    {% set category = UI.renderUrl(path('orob2b_catalog_category_update', {'id': entity.category.id}), entity.category.defaultTitle) %}
{%- elseif entity -%}
    {% set category = entity.category ? entity.category.defaultTitle : '' %}
{%- else -%}
    {% set category = '' %}
{%- endif -%}
{{ UI.renderHtmlProperty('orob2b.catalog.category.entity_label'|trans, category) }}
