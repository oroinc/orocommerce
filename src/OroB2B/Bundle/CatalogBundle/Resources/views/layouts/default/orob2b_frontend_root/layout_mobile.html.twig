{% block _hero_promo_widget %}
    {% set attr = layout_attr_defaults(attr, {
        '~data-page-component-options': {
            autoplay: false,
            arrows: false,
            dots: true
        }
    }) %}

    {{ block_widget(block, {
        attr: attr
    }) }}
{% endblock %}
