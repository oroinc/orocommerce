{% import 'OroUIBundle::macros.html.twig' as UI %}

{%- if entity and resource_granted('EDIT', entity) -%}
    {% set productTaxCode = UI.renderUrl(path('orob2b_tax_product_tax_code_update', {'id': entity.id}), entity.code) %}
{%- elseif entity -%}
    {% set productTaxCode = entity ? entity.code : '' %}
{%- else -%}
    {% set productTaxCode = '' %}
{%- endif -%}
{{ UI.renderHtmlProperty('orob2b.tax.producttaxcode.entity_label'|trans, productTaxCode) }}
