layout:
    actions:
        - @appendOption:
            id: title
            optionName: value
            optionValue: { @value: $context.title }
    conditions:
        @not_empty: [ $context.title ]
