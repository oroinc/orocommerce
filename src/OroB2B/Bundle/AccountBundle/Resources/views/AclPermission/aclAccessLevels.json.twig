{
{% set translatedLevels = {} %}
{% for id, level in levels %}
    {% if level is empty %}
        {% set level = 'NONE' %}
    {% endif %}
    {% set label = roleTranslationPrefix ~ level %}
    {% if loop.index != loop.first %},{% endif %}
    "{{ id }}":"{{ label|trans }}"
{% endfor %}
}
