layout:
    actions:
        - @setBlockTheme:
            themes: 'OroB2BCheckoutBundle:layouts:default/orob2b_checkout_frontend_checkout/templates/main.html.twig'
        - @addTree:
            items:
                checkout_container:
                    blockType: container
                checkout_sidebar:
                    blockType: block
                    options:
                        vars:
                            page: { @value: $data.page }
                            checkout: { @value: $data.checkout}
                checkout_content:
                    blockType: container
                checkout_information:
                    blockType: container
                checkout_order_summary:
                    blockType: container
                    options:
                        visible: { @neq: [$context.page, 5] }
                        vars:
                            summary_data: { @value: $data.summary.data}

                checkout_order_summary_table:
                    blockType: block
                    options:
                        vars:
                            summary_data: { @value: $data.summary.data}
            tree:
                main:
                    checkout_content:
                        checkout_information: ~
                        checkout_order_summary:
                            checkout_order_summary_table: ~
                    checkout_sidebar: ~

