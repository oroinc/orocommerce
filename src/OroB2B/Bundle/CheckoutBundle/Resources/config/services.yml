parameters:
    orob2b_checkout.layout.data_provider.checkout.class: OroB2B\Bundle\CheckoutBundle\Layout\DataProvider\CheckoutDataProvider
services:
    orob2b_checkout.layout.data_provider.account_user_checkouts:
        class: %orob2b_checkout.layout.data_provider.checkout.class%
        arguments:
            - @oro_workflow.manager
        tags:
            - { name: layout.data_provider, alias: orob2b_checkout_checkout }