{% extends 'OroUIBundle:actions:update.html.twig' %}
{% form_theme form with ['OroFormBundle:Form:fields.html.twig'] %}

{% oro_title_set({params : {"%title%": entity|default('N/A'|trans)} }) %}

{% set formAction = path('orob2b_rfp_request_update', { 'id': entity.id }) %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity':      entity,
        'indexPath':   path('orob2b_rfp_request_index'),
        'indexLabel':  'orob2b.rfp.request.entity_plural_label'|trans,
        'entityTitle': entity|default('N/A'|trans)
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block navButtons %}
    {{ UI.cancelButton(path('orob2b_rfp_request_index')) }}
    {% set html = UI.saveAndCloseButton() %}
    {% if entity.id or resource_granted('orob2b_rfp_request_create') %}
        {% set html = html ~ UI.saveAndStayButton() %}
    {% endif %}
    {{ UI.dropdownSaveButton({'html': html}) }}
{% endblock navButtons %}

{% block content_data %}

    {% set id = 'request-edit' %}

    {% set dataBlocks = [
        {
            'title': 'orob2b.rfp.sections.general'|trans,
            'class': 'active',
            'subblocks': [{
                'title': '',
                'data': [
                    include('OroB2BRFPBundle:Request:block/infoRequest.html.twig'),
                ]
            }]
        },
        {
            'title': 'orob2b.rfp.sections.request_products'|trans,
            'subblocks': [{
                'title': '',
                'data': [
                    form_row(form.requestProducts)
                ]
            }]
        }
    ]
    %}

    {% set additionalData = [] %}
    {% for child in form.children if child.vars.extra_field is defined and child.vars.extra_field %}
        {% set additionalData = additionalData|merge([form_row(child)]) %}
    {% endfor %}

    {% set data = {
        'formErrors': form_errors(form),
        'dataBlocks': dataBlocks
    }%}

    {{ parent() }}
{% endblock content_data %}
