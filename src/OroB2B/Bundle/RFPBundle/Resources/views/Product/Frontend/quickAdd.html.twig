{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.buttonType({
    type: 'submit',
    label: 'orob2b.rfp.btn.add_to_order'|trans,
    class: 'component-button',
    data: {
        'component-name': 'orob2b_rfp_quick_add_processor'
    }
}) }}
