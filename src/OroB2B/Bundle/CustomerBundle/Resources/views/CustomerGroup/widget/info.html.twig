{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEmailBundle::actions.html.twig' as EmailActions %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('orob2b.customer.customergroup.name.label'|trans, entity.name) }}

            {% if entity.paymentTerm and is_granted('VIEW', entity.paymentTerm)%}
                {{ UI.renderProperty('orob2b.customer.customergroup.payment_term.label'|trans, entity.paymentTerm.label) }}
            {% endif %}
        </div>
    </div>
</div>
