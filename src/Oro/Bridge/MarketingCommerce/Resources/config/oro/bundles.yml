bundles:
    # needs to be loaded after MarketingListBundle's priority: 20
    # needs to be loaded after TrackingBundle's priority: 25
    # needs to be loaded after CampaignBundle's priority: 30
    - { name: Oro\Bridge\MarketingCommerce\OroMarketingCommerceBridgeBundle, priority: 31 }
