data:
    type: productunits
    id: '<toString(@day->code)>'
    attributes:
        defaultPrecision: 1
        label: day
        shortLabel: d_upd
        pluralLabel: days
        shortPluralLabel: ds_upd
