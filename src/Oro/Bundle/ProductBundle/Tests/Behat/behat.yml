oro_behat_extension:
  elements:
    ProductNameField:
      selector: "input[name='oro_product[names][values][default]']"

    SlugPrototypesField:
      selector: "input[name='oro_product[slugPrototypesWithRedirect][slugPrototypes][values][default]']"

    QuickAddForm:
      selector: "form[name='oro_product_quick_add']"

    CreateOrderButton:
      selector: "a[title='Create Order']"
