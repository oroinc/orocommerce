{% import 'OroUIBundle::macros.html.twig' as UI %}

<div class="pull-right">
    {{ UI.clientLink({
        'dataUrl': path('oro_product_possible_products_for_related_products', {'id': entity.id}),
        'aCss': 'btn no-hash',
        'label' : 'oro.product.widgets.select_related_products.label'|trans,
        'widget' : {
            'type' : 'related-items',
            'options' : {
                'dialogOptions' : {
                    'title': 'oro.product.widgets.select_related_products.title'|trans({'{{ sku }}': entity.sku, '{{ name }}': entity.name}),
                    'modal': true,
                    'resizable': false,
                    'width': 820,
                    'height': '80vh',
                    'autoResize': true,
                },
                'itemsLimit': relatedProductsLimit,
                'itemsIdsToAdd': '#productAppendRelated',
                'itemsIdsToRemove': '#productRemoveRelated',
            }
        }
    }) }}
</div>
