{% if oro_is_single_unit_mode_code_visible() or not oro_is_default_primary_unit_code(record.getValue(unitColumnName)) %}
    {{ record.getValue(quantityColumnName)|oro_format_decimal|oro_format_product_unit_value(record.getValue(unitColumnName), isShortUnitCode) }}
{% endif %}

