{% if oro_is_unit_code_visible(record.getValue(unitColumnName)) %}
    {{ record.getValue(quantityColumnName)|oro_format_decimal|oro_format_product_unit_code(record.getValue(unitColumnName), isShortUnitCode) }}
{% else %}
    {{ record.getValue(quantityColumnName)|oro_format_decimal }}
{% endif %}

