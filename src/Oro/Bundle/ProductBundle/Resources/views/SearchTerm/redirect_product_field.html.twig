{% import '@OroUI/macros.html.twig' as UI %}

{{ UI.renderHtmlProperty(
    'oro.websitesearchterm.searchterm.redirect_product.label'|trans,
    UI.entityViewLink(entity, entity.defaultName, 'oro_product_view', 'oro_product_view')
) }}
