layout:
    actions:
        - '@move':
            id: product_datagrid_row_product_shopping_lists
            parentId: product_datagrid_row_product_primary_content_container
            prepend: true

    conditions: 'context.has("theme_name") && context["theme_name"] == "list-view"'
