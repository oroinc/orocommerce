layout:
    actions:
        - '@move':
            id: checkout_additional_options
            parentId: checkout_form_fields

    conditions: 'context["workflowStepName"]=="order_review" && context["is_mobile"]'
