import ParentDialogWidget from 'oroaddressvalidation/js/app/views/frontend-address-validation-dialog-widget';

const CheckoutAddressValidationDialogWidget = ParentDialogWidget.extend({});

export default CheckoutAddressValidationDialogWidget;
