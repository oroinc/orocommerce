{# @name = checkout_customer_user_reset_password #}
{# @entityName = Oro\Bundle\CustomerBundle\Entity\CustomerUser #}
{# @subject = Reset Account User Password #}
{# @isSystem = 1 #}
{# @isEditable = 1 #}

{% extends oro_get_email_template('base_storefront') %}

{% block content %}
    <h1 style="{{ styleH1 }}">Hello, {{ entity.username }}!</h1>

    <!--empty space-->
    <table role="presentation" border="0" cellspacing="0" cellpadding="0">
        <tr>
            <td height="12" style="{{ styleEmptyRowH12 }}">&nbsp;</td>
        </tr>
    </table>

    <p style="{{ styleP }}">To reset your password - please visit
        <a class="link" style="{{ styleLink }}"
           href="{{ website_path('oro_customer_frontend_customer_user_password_reset', { token: entity.confirmationToken, input_action: redirectParams }) }}">this
            link</a>
    </p>
{% endblock %}
