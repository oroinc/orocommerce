{% block  _shopping_list_matrix_form_start_widget %}
    {% if form.rows[0].columns|length == 1 %}
        {% set attr = layout_attr_defaults(attr, {
            '~class': class_prefix ~ '__form--inline '
        }) %}
    {% endif %}

    {{ parent_block_widget(block) }}
{% endblock %}
