{% import '@OroUI/macros.html.twig' as UI %}
{% if is_granted('oro_product_inventory_update') and is_granted('EDIT', entity) %}
    {% set dialogTitle %}
        {{ entity.sku|default('N/A'|trans) }} - {{ entity.defaultName.string|default('N/A'|trans) }} - {{ 'oro.inventory.widgets.inventory_management'|trans }}
    {% endset %}
    {{ UI.clientLink({
        'dataUrl': path('oro_inventory_level_update', {'id': entity.id}),
        'aCss': 'dropdown-item no-hash',
        'iCss': 'fa-list-alt',
        'label' : 'oro.inventory.widgets.inventory_management'|trans,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
                'dialogOptions' : {
                    'stateEnabled': false,
                    'incrementalPosition': false,
                    'title' : dialogTitle,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 800,
                }
            }
        }
    }) }}
{% endif %}
