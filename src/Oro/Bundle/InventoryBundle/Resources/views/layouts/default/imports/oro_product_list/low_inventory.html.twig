{% block __oro_product_list__product_low_inventory_label_widget %}
    {% if (oro_is_low_inventory_product(product)) %}
        <div class="grid">
            <div class="grid__row">{{ "oro.inventory.low_inventory.label"|trans }}</div>
        </div>
    {% endif %}
{% endblock %}
