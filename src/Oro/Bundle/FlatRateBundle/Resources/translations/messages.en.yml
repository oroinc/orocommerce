oro:
    flat_rate:
        settings:
            label: Flat Rate Transport Settings
            labels.label: Label

        channel_type:
            label: Flat Rate Shipping
        form:
            oro_flat_rate_options_type:
                label: Flat Rate
        method:
            price.label: Price
            handling_fee.label: Handling Fee
            processing_type:
                label: Type
                per_item.label: Per Item
                per_order.label: Per Order

        rule:
            deactivate:
                massage: Some shipping methods in <a href="{{ linkGrid }}" target="_blank">these</a> shipping rules depend on the integration that is being disabled. The affected shipping methods will be disabled. Shipping rule will also be disabled if none of its shipping methods remain enabled.
                title: Disabling linked shipping rules and methods.
                button.okText: Deactivate
            delete:
                massage: Some shipping methods in <a href="{{ linkGrid }}" target="_blank">these</a> shipping rules depend on the integration that is being deleted. The affected shipping methods will be disabled. Shipping rule will also be disabled if none of its shipping methods remain enabled.
                title: Deleting linked shipping rules and methods.
                button.okText: Delete
