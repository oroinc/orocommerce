{% import '@OroDataGrid/macros.html.twig' as dataGrid %}
{% import '@OroUI/macros.html.twig' as UI %}

{% if is_granted('oro_order_create') %}
    {{ UI.renderButtonsRow(UI.quickAccessAddButton({
        'dataUrl': path('oro_order_create_for_customer', { customer: entity.id }),
        'title': 'oro.order.customer.quick_access_button.new_order.label'|trans,
        'label': 'oro.order.customer.quick_access_button.new_order.label'|trans,
        'widget': {
            'reload-grid-name': 'customer-orders-grid',
            'options': {
                'alias': 'oro_order_create_for_customer_dialog',
                'dialogOptions': {
                    'title': 'oro.order.customer.quick_access_button.create_order.widget.title'|trans,
                }
            }
        }
    })) }}
{% endif %}

{{ dataGrid.renderGrid('customer-orders-grid', {customer_id: entity.id}, {cssClass: 'inner-grid'}) }}
