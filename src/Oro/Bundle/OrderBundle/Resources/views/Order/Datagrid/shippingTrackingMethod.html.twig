{% set shippingMethod  =  record.getValue('method') %}
{%- if shippingMethod -%}
    {{ oro_order_format_shipping_tracking_method(shippingMethod)|trans }}
{%- else -%}
    {{ 'oro.ui.empty'|trans }}
{%- endif -%}
