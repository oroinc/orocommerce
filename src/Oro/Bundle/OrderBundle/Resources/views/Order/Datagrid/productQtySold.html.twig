{% set qtySold = record.getValue('qtySold') %}
{% set unitCode = record.getValue('unitCode') %}
{% if qtySold -%}
    {{ qtySold }} {{ unitCode|oro_format_product_unit_label(false, qtySold > 1)|capitalize }}
{%- endif %}
