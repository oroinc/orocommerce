{% set shippingMethod = record.getValue('shippingMethod') %}
{%- if shippingMethod -%}
    {{ get_shipping_method_type_label(shippingMethod, record.getValue('shippingMethodType'), record.getValue('organizationId'))|trans }}
{%- endif -%}
