define(function(require) {
    'use strict';

    var WebCatalogSlugifyComponent;
    var LocalizedSlugifyComponent = require('ororedirect/js/app/components/localized-slugify-component');

    WebCatalogSlugifyComponent = LocalizedSlugifyComponent.extend({
    });

    return WebCatalogSlugifyComponent;
});
