<div class="control-group">
    <label class="control-label">{{ 'oro.payment.status.label'|trans }}</label>
    <div class="controls">
        <div class="control-label">
            {% if data is not empty %}
                {{ get_payment_status_label(data) }}
            {% else %}
                {{ 'oro.ui.empty'|trans }}
            {% endif %}
        </div>
    </div>
</div>
