{% apply spaceless %}
    {% set destinations = record.getValue('destinations').toArray() %}

    {% set escapedDestinations={} %}
    {% for key, destination in destinations %}
        {% set escapedDestinations = escapedDestinations|merge({(key): destination|e}) %}
    {% endfor %}

    {{ escapedDestinations|join('</br>')|raw }}
{% endapply %}
