data:
    type: customergroups
    id: '<toString(@customer_group.group1->id)>'
    attributes:
        name: customer_group.group1
    relationships:
        customers:
            data:
                -
                    type: customers
                    id: '<toString(@customer.1->id)>'
