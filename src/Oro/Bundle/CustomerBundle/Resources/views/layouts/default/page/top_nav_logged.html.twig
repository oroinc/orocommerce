{% block _top_nav_left_logged_user_widget %}
    {% set attr = layout_attr_defaults(attr, {
    '~class': ' topbar-navigation__item'
    }) %}

    <li {{ block('block_attributes') }}>
        {% for child in block %}
            {{ block_widget(child) }}{% if not loop.last %} {% endif %}
        {% endfor %}
    </li>
{% endblock %}

{% block _top_nav_left_sign_out_item_widget %}
    {% set attr = layout_attr_defaults(attr, {
    '~class': ' topbar-navigation__item'
    }) %}

    <li {{ block('block_attributes') }}>
        {{ block_widget(block) }}
    </li>
{% endblock %}

{% block _top_nav_left_my_account_item_widget %}
    {% set attr = layout_attr_defaults(attr, {
    '~class': ' topbar-navigation__item'
    }) %}

    <li {{ block('block_attributes') }}>
        {{ block_widget(block) }}
    </li>
{% endblock %}

{% block _top_nav_left_sign_out_link_widget %}
    {% set attr = layout_attr_defaults(attr, {
    '~class': ' topbar-navigation__link'
    }) %}

    {{ block_widget(block, {attr: attr}) }}
{% endblock %}

{% block _top_nav_left_my_account_link_widget %}
    {% set attr = layout_attr_defaults(attr, {
    '~class': ' topbar-navigation__link'
    }) %}

    {{ block_widget(block, {attr: attr}) }}

{% endblock %}
