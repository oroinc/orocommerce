{% import '@OroUI/macros.html.twig' as UI %}

{{ UI.renderHtmlProperty(
    'oro.websitesearchterm.searchterm.redirect_category.label'|trans,
    '<a href="' ~ path('oro_catalog_category_update', { id: entity.id }) ~ '">' ~ entity.defaultTitle|escape ~ '</a>'
) }}
