{% block _top_nav_container_widget %}
    {% set attr = layout_attr_defaults(attr, {
        '~class': " topbar"
    }) %}
    <div {{ block('block_attributes') }}>
        <div class="container-fluid">
            <div class="row">
                {{ block_widget(block) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block _top_nav_widget %}
    {% spaceless %}
        {% set attr = layout_attr_defaults(attr, {
            '~class': " topbar__navigation topbar__navigation--has-sibling"
        }) %}
        {% set move_options =  {
            responsive: [
            {
                breakpoint: 1024,
                moveTo: '.quick-access__list'
            }
            ]}|json_encode
        %}

        <nav {{ block('block_attributes') }}
                data-move-block
                data-move-options="{{ move_options }}"
        >
            {{ block_widget(block) }}
        </nav>
    {% endspaceless %}
{% endblock %}

{% block _top_nav_menu_container_widget %}
    {% set attr = layout_attr_defaults(attr, {
        '~class': " col-md-7 col-sm-7 text-right"
    }) %}
    <div {{ block('block_attributes') }}>
        {{ block_widget(block) }}
    </div>
{% endblock %}


{% block _top_nav_controls_widget %}
    {% set attr = layout_attr_defaults(attr, {
        '~class': " col-md-5 col-sm-5"
    }) %}
    {% set move_options =  {
        responsive: [
            {
                breakpoint: 1400,
                moveTo: '.header-row__toggle'
            },
            {
                breakpoint: 1300,
                MoveTo: '.middlebar'
            }
        ]}|json_encode
    %}

    <div {{ block('block_attributes') }}>
        <nav class="topbar__navigation"
             data-move-block
             data-move-options="{{ move_options }}"
        >
            <ul>
                {{ block_widget(block) }}
            </ul>
        </nav>
    </div>
{% endblock %}
