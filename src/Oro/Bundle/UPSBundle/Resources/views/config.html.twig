{% set methodLabel = get_shipping_method_label(methodConfig.methodIdentifier)|trans %}
{% if methodLabel|length > 0 %}
<li>{{ methodLabel }}
    <ul>
{% endif %}

        {% for typeConfig in methodConfig.typeConfigs %}
        <li>{{ get_shipping_method_type_label(methodConfig.methodIdentifier, typeConfig.type)|trans}}
            {% if typeConfig.options.surcharge is not empty %}
            (   {{ 'oro.ups.form.shipping_method_config_options.surcharge.label'|trans}}: {{ typeConfig.options.surcharge }})
            {% endif %}
        </li>
        {% endfor %}

{% if methodLabel|length > 0 %}
    </ul>
</li>
{% endif %}

