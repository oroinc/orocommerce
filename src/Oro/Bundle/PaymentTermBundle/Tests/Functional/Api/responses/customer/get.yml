data:
    type: customers
    id: '<toString(@customer.level_1->id)>'
    relationships:
        paymentTerm:
            data:
              type: paymentterms
              id: '<toString(@payment_term_test_data_net 10->id)>'
