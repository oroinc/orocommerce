{% set params = oro_action_widget_parameters() %}

{% if has_actions(params) %}
    {{ oro_widget_render({
        'widgetType': 'buttons',
        'url': path('oro_action_widget_buttons', params),
        'alias': 'actions_buttons'
    }) }}
{% endif %}
