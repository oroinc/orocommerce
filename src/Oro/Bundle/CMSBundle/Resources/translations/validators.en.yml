oro.cms:
    contentblock:
        alias:
            regex.message: "This value should contain only alphabetic symbols, underscore, hyphen and numbers."
    image_slider:
        url:
            not_dangerous_protocol.message: '"{{ protocol }}" pseudo-protocol is not allowed in slide URIs.'
