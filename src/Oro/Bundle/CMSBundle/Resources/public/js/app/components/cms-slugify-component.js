define(function(require) {
    'use strict';

    var CMSSlugifyComponent;
    var LocalizedSlugifyComponent = require('ororedirect/js/app/components/localized-slugify-component');

    CMSSlugifyComponent = LocalizedSlugifyComponent.extend({
    });

    return CMSSlugifyComponent;
});
