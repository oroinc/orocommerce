{% block content_block_widget %}
    <div{{ block('block_attributes') }}>
        {%- if not hideTitle|default(false) -%}
            <h3>{{ contentBlock.titles|localized_value }}</h3>
        {%- endif -%}
        {{ contentBlock.content|oro_html_sanitize|render_content }}
    </div>
{% endblock %}
