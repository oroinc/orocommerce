{% import '@OroUI/macros.html.twig' as UI %}

{{ UI.renderHtmlProperty(
    'oro.tax.taxcode.label'|trans,
    UI.entityViewLink(entity, entity ? entity.code : null, 'oro_tax_product_tax_code_view')
) }}
