{% extends 'OroUIBundle:actions:index.html.twig' %}

{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set gridName = 'tax-taxes-grid' %}
{% set pageTitle = 'oro.tax.entity_plural_label'|trans %}

{% block navButtons %}
    {% if resource_granted('orob2b_tax_create') %}
        <div class="btn-group">
            {{ UI.addButton({
                'path': path('orob2b_tax_create'),
                'entity_label': 'oro.tax.entity_label'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}
